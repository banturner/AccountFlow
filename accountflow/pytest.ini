[pytest]
# Make `import app` work whether the suite is run as `pytest` or
# `python -m pytest`: tests/ has no __init__.py, so without this pytest puts
# tests/ on sys.path but not the project root.
pythonpath = .
testpaths = tests
